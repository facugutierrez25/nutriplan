# Testcontainers levanta un PostgreSQL real y descartable para cada corrida de tests.
spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///nutriplan
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
